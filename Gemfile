source 'https://rubygems.org'

# To use the github-pages bundle
#require 'json'
#require 'open-uri'
#versions = JSON.parse(open('https://pages.github.com/versions.json').read)
#gem 'github-pages', versions['github-pages']
gem 'github-pages'

# For Muffin as it uses sass & scss
gem 'sass'

# To bypass an error because of the dependency of coffeescript
gem 'therubyracer'
